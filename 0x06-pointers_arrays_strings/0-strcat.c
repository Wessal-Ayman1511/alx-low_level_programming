#include"main.h"
#include<string.h>
/**
*_strcat- function that concatenates two strings.
*
* @dest: input
*
* @src: input
*
* Return: return(poiter)
*/
char *_strcat(char *dest, char *src);
{
	int *p;

	p = strcat(dest, src);
	return (p);
}
